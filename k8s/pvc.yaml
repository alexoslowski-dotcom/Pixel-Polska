apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pixel-polska-data-rwx
  namespace: pixel-polska
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: rwx-storage
