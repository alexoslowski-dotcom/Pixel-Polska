apiVersion: v1
kind: Secret
metadata:
  name: pixel-polska-secrets
  namespace: pixel-polska
type: Opaque
stringData:
  # In production replace with secure credentials or external secret manager.
  DATABASE_URL: "postgres://user:password@postgres:5432/pixel_polska"
